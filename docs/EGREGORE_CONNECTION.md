# RailScout ↔ UNIIMENTE Kernel Connection

RailScout is a local organ repository. The UNIIMENTE Kernel remains the constitutional control plane.

## Canonical relationship

- **Kernel:** `dababiyoda/uniimente-kernel`
- **RailScout organ repo:** `dababiyoda/RAILSCOUT`
- **Kernel issue preserving this conversation:** `dababiyoda/uniimente-kernel#75`
- **Kernel draft PR canonizing RailScout:** `dababiyoda/uniimente-kernel#72`

## Authority model

RailScout does not own its own constitutional authority. It consumes governance from the Kernel.

Local RailScout files may define:

- organ-specific doctrine;
- workstation specifications;
- local founder-intent records;
- implementation plans;
- operator instructions;
- evidence, validation, and tool-use procedures.

Local RailScout files must not independently grant:

- legal authority;
- spending authority;
- external-action authority;
- autonomy promotion;
- capability grants;
- irreversible workspace or repository changes;
- permission to bypass Kernel gates.

## Integration rule

The Kernel decides what may become real. RailScout refines signals, evidence, opportunities, architectures, validation pathways, and tool-orchestration workflows into proposals and governed records.

RailScout must emit structured outputs that the Kernel or approved human operator can evaluate, including:

- context packets;
- evidence packets;
- contradiction reports;
- stakeholder maps;
- opportunity hypotheses;
- threat packets;
- validation experiments;
- candidate architectures;
- adversarial elimination reports;
- synthesis records;
- highest-state ideas;
- workstation build records;
- tool/app lessons;
- cadence run notes.

## Local-to-kernel synchronization

When RailScout doctrine or implementation changes materially:

1. Update the local RailScout Founder Intent Ledger.
2. Link the relevant Kernel issue, PR, or contract.
3. Preserve evidence and dissent.
4. Use the Recursive Collaboration Protocol.
5. Avoid side-canon documentation.
6. Do not claim Kernel implementation until the Kernel repo is actually updated and verified.

## Current synchronization target

The Kernel's RailScout organ registry should reference this repository as the RailScout organ implementation surface:

```yaml
repository: dababiyoda/RAILSCOUT
```

Until the Kernel PR is merged, this local repository should treat that connection as proposed or pending, not fully implemented in `main`.
